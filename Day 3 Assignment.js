/*  
const browserVersion = "Chrome"

const getbrowserversion = () =>{
 
    if (browserVersion === "Chrome") {
        let browserVersion = "1.2.3"
        console.log('browserVersion inside if block:', browserVersion);
        
    }

    console.log('browserVersion outside if block:', browserVersion);
    
}

getbrowserversion();
*/

const browserVersion = "chrome"

function getbrowserversion() {
    if (browserVersion =="chrome") {
        var browserVersion = "edge"
        console.log("browserVersion inside if block", browserVersion);
        
    }
    console.log("browserVersion outside the if block", browserVersion);
    
}
getbrowserversion();
